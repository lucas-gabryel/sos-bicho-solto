import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Detalhe do tutor',
};

export default function TutorDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
